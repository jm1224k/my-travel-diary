import React, { Component } from "react";
import { View, Text, TouchableOpacity, StyleSheet } from "react-native";

export default class Trip extends Component {
  render() {
    return (
      <View>
        <Text>Hello I am trip!</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
